export interface IRegistro {
  id: number;
  intervencion: string;
  numeroHistoriaClinica: string;
  fecha: string;
}

export interface IMaterial {
  id: number;
  nombre: string;
  tipo: string;
  codigo: string;
}
